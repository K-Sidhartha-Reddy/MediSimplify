# Public API base; default is /api which proxies to BACKEND_ORIGIN
NEXT_PUBLIC_API_URL=https://placeholder.vercel.app

# Backend origin used by Next.js rewrite (dev/prod)
BACKEND_ORIGIN=http://localhost:8000
